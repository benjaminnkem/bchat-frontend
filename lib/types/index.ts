export interface User {
  email: string;
  username: string;
  firstName: string;
  lastName: string;
  department: string;
  level: string;
  school: string;
}

export interface Message {
  name: string;
  text: string;
}
